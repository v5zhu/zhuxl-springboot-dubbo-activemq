#spring.application.name=dubbo-consumer
server:
  port: 8102
  servlet:
    context-path: /order
  #应用名称
spring: 
  dubbo:
    application:
      name: auth-consumer
#注册中心地址
    protocol:
      name: dubbo
    registry:
      address: zookeeper://47.96.28.236:2181
#调用dubbo组建扫描的项目路径
    scan: com.demo.controller
